/*
 *
 * WeatherContainer constants
 *
 */

const common = 'app/WeatherContainer/';

export const SET_NEW_WEATHER = `${common}SET_NEW_WEATHER`;
export const WEATHER_LOADING = `${common}WEATHER_LOADING`;
export const WEATHER_LOADED = `${common}WEATHER_LOADED`;
